// Enums are used to define a set of named constants.

const enum seatChoice {
  window,
  aisle,
  middle,
  Fourth
}

const vSeatChoice = seatChoice.Fourth; // Using this enum we can make the option for the user to choose a seat type.

export {};