const superHero: string[] = [];
// const heroPower: number[] = [];
//Another way to define an array is.
const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
}

const allUser: User[] = [];

// We can also create array inside array.
const MLModels: number[][] = [
  [233, 233,554],
  [234,7543, 453]
]
superHero.push("Iron_Man");
heroPower.push(100);

allUser.push({name: "vivek", isActive: true});